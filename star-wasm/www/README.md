STAR in the browser with WebAssembly!

## Running

```sh
make build -C ../wasm && npm install && make release && node server.js
```

Then visit: `http://localhost:8084`.

Open dev tools and check `console`.
